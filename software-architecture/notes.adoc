= Notes

== Goodbye Microservices, Hello Self-Contained Systems - Simon Martinelli

Notes taken from https://www.youtube.com/watch?v=hW22FxcrtkA

2010 => Netflix pushed for microservices

They created a **big ball of mud**. Mainly because they didn't understand microservice
architecture well.

**Misconception**

- Monolithic Applications = Big Ball of Mud

It could be a Big Ball of Mud, or it could be glorious. You can build _Modular
Monoliths_ in a modular way. Java does a bit of that, _but it might not be a
good idea_.

A good idea to keep in mind:

[quote, David L. Parnas, On the criteria to be used in decomposing systems into modules]
----
The **effectiveness of a modularization** is dependent upon the **criteria used in
dividing** the system into modules
----

If you have a lot of coupling within the modules, then things might be useless.

It's all about Modularity.

**Module**

Modules can be in microservices, in packages, in components. They all share the
same definitions:

A self-contained **component** of a system, often **interchangeable**, which has
a **well-defined interface** to the other components.

But let's move now to **Microservices**

- Naming is hard, however using **microservices** to talk about modules that can be
an error.
	- Micro is not micro, it just means "smaller"
	- Domain Driven Design => Bounded Contexts was a good concept to borrow. It
	is a framework to split a big domain into subdomains. You can separate a
	business by some form. _The organization of the company is a good starting
	point to see how you can define your bounded contexts_

_One issue with MS:_

Some customers have so many microservices, that they can't even update easily
to the newest technologies and they realize late that they could've just put them
all together for simplicity.

**Interesting idea**

- Even if your app is a Big Ball of Mud, you can use tools, and nowadays AI
to re-engineer your application so it's more modular, separating concerns
better and making it more maintainable and extendable.

Now, if you have _a distributed big ball of mud_. Then that's much more complicated.
There's people that are doing distributed tracing in production just to find out
what microservice calls to what other microservices.

[quote, Simon Brown]
----
If you can't build a modular monolith, what makes you think microservices are the
answer?
----

In separating things into separate services, you could even make an application
_slower_.

[quote, Martin Fowler]
----
First Law of Distribution: Don't Distribute
----

By distributing things you might end up with:

- Higher latency
- Security concerns
- Problems with resilience
- Network might not be fast/reliable

If you do communication between microservices:

- Synchronous => REST services
	- Netflix doesn't use REST anymore, they do GraphQL for Front, gRPC for backend
- Asynchronous => Helps with resilience (don't wait for response)
	- You send the request, and at some point you get the reply
	- But what do you do if you wait for it and don't get an answer?
		- Send request again
			- If the consumer is not idempotent, things are done twice
- Event based => Fire and forget
	- Things get consumed at some point, and perhaps further events are created
	based on the process of the previous ones
	- But apps become more resilitient as a consequence
	- **But there's a price**, you need some messaging system. You need something
	like RabbitMQ, Apache Kafka. This has to be maintained, upgraded. So _NOT FOR
	FREE_

=== Anti-Patterns

Integration Database = Multiple Microservices are accessing the same schema on
the database, and you have issues of: Who does migrations? Who's the owner of
the data? Who's writing the data? - So please, don't do it.

However, the **Integration Database** can become an intermediate step from a monolith
to a more modular system.

Moving the backend to modularity and microservices might solve different issues,
however the bottleneck can easily drift into the Frontend. The Frontend might
become this huge monolith too.

_Note:_ So companies are moving to micro-frontends, but apparently at the time
of writing (2026-June) there really aren't mature frontends to really take on
the whole micro-frontends pattern in stride.

=== Self-Contained Systems Architecture

Apparently, this pattern was introduced at the same time that Netflix did the
microservices inception.

SCS:

- Cut the monolith along bounded contexts
- Wrap each domain in its own web application
- That application is a **Self-contained System (SCS)**

We don't create just a backend service, we create the whole application and that's
called Self Contained Systems (SCS)

**German speaking side:** Seems to know well about this.

_Characteristics:_

- An SCS contains its own **user interface**, specific **business logic**, and **separate
data storage**
	- It may contain an _optional API_ (preferably async)
	- This becomes even bigger than a microservice, and in turn, the communication
	across "micro-services" ends up being less important

So, you have the whole app in **one silo**.

**Data Storage**

- Every SCS brings its **own data storage**, which may cause **redundant data**
- Redundancy is tolerable as long as **data sovereignty** is preserved
	- So just one system should write, read

**How to Integrate SCS?**

- Self-contained Systems should be integrated via their **web interfaces** to
minimize coupling to other systems.
	- Basically, you are going to go back to good 'ol **hyperlinks**
		- You click on one place, and that navigates away to another SCS

You might be thinking: _Ah, that's so bad, my users want seamless interactions and
great integration_. But if you put some telemetry in place, you might be surprised
the amount of people that will stay within **one silo**

=== Asynchronous for the Win!

- **Synchronous** remote calls inside business logic should be avoided
- **Asynchronous** calls **reduce dependencies** and prevent error cascades
- **Consistency** guarantees **may be relaxed**, depending on the required update
rate

- So eventual consistency will be introduced
	
The advantages of SCD => Are the sames as microservices

- Resilient
- Scalable
- Open
- Independent

The most important part of modular architecture styles is the modularity.

Modernization => Monoliths to turn into modules.

- Scaling to Zero => An interesting strategy to save up on costs, is the fact that
on weekends when people don't use the systems you can scale back everything and
save up on some power
	- Making things modular allows for you to choose whatever tech stack you want,
	and also plug-and-play solutions if you choose so

**AI Needs Context**

- AI assistants are only as good as the context they see

**With microservices**

- One feature lives in many repos
- The full code may not fit in the context window
- AI suggestions break other services
- Humans still stitch it together

In this age of AI and Agentic coding, if a context is **too big** or **too chaotic**.
The AI itself might have a hard time really understanding it, and in turn waste
more time (even though they are amazing at reading and processing tons of information
and fast).

**SCS: Right-Sized for AI**

- **The full picture fits**
	- UI, business logic, and data access live together
	- The whole context fits in the window

**Changes stay inside the boundary**

- One team per SCS maps to one AI agent per SCS

So, you can program, maintain, understand, and iterate over an end-to-end
architecture, which brings in advantages such as less fragmentation, more locality,
less cognitive overload.

=== Modernizing a Wholesale ERP System

This is a DEMO of how a customer project by using SCS works and how it's been
developed.

An old homehrown ERP System. People don't want to use applications that must
be installed, UI is outdated, so a need for modernization was needed.

The domains that were done:

- Procurement
- Sales
- Logistics
- Finance

All the parts are there, logically. The first step:

- Split the monolithic application into the **high level subdomain struct**

Apparently they have about 30 SCS running to serve this modernized iteration.

And they leverage "DDD" because they are using "Bounded Context" to describe
how each SCS makes usage of their data to do their assigned work.

Everything is **events**, that get published to a topic/queue, and that then
get passed around amongst domains. They then take the data inside the events,
and update their databases accordingly, on top of reacting however they need
to react. Another big thing is that separated to these business logic events,
there's solely **database events**, replication. That happens amongst the
multiple databases, Even if you have read-only or write-only databases, you need
to replicate some level of data to keep consistency, and in case you need (data
RIGHT NOW), you would need to go through the topic/queue intermediary, and perhaps
have a bigger delay than what a sync endpoint call would do.

**Macro Architecture**

- **Data integration**
	- **Replication** of read-only data based on database views
	- Async through **events**
- **User Interface**
	- Common **Design Systems** with Components and Styles
	- **Link database** for navigation

SCS are **completely independent systems**. One can be written on JS, other on
Go, other on C#, other in JAVA. However, the decisions that was taken was as
follows:

````
- Because we want to exchange developers from different teams, all the application's
stack will be with the same framework/language.
````

In this specific use case, the different SCS were actually being used by different
users, and different departments, so they wouldn't really start navigating across
SCSs, **however**, the team still decided to run with a **Design System** so
all of them share a visual line.

**Data Redundancy**

There are two ways:

- You do **Replication**
	- **Pull-based** replication
	- **Target system refreshes data**
	- Good for **data** that **does not change** very **often**
	- Adds **delay** depending on refresh interval
	- **Simple** to implement
- You do **Events**
	- **Push-based** replication
	- **Source system publishes changes**
	- Good for **frequently changing data**
	- **Near-real-time** updates
	- **Higher** operational **complexity**
		- Someone has to manage the messaging system, the Kafla Cluster, etc

So, an interesting strategy for migration is as follows:

````
In moving the old system to the new SCS architecture. We want to keep running the
old systems as long as possible (yet, knowing that they will be gone eventually),
so that if something goes wrong with the new system. Users can fallback to the old
system.
````

The idea is to connect an old database with the new system (at least for the time
being), since the team sometimes doesn't know how many SCS will be there, it really
depends as you go, how you analyze, what you encounter, the issues and challenges
that start showing up the more you dig further. But the idea, always, is that the
old database will be gone as well eventually.

_Migration of a system sometimes takes longer than you envision_. Most of the time
is because you need the experts, the domain experts to chime in and help out.

_Management might think something will take 3 years_. But internally the team
might already have a rodemap for 5 years.

**Strangler Fig Pattern**

This is a very good illustration of how the migration that is taking place on this
talk's example. The new system, is running in parallel, slowly but surely workings
its away until it takes over.

This reduces risk also, because there's less "big applications" that the team
has to take care of.

The code is not "translated" => The requirements are reverse engineered, then
those are presented to stakeholders and we ask: "Are these requirements still
valid?". Then code starts getting migrated.

- Beginning: Management said "NO NEW FEATURES"
	- Once you are done, add new features
		- However during the process, the dev team already started implementing
		new features. This is positive on the user's side because they finally
		get features they were waiting for already for many years

**Conclusion**

- _Optimal Modularity_ based on business boundaries
	- If you do modules, always check how the users use the application, how the
	domains can be split out, that way you reduce communication between bounded
	contexts. It's better to have as least of communication as possible.
- _Team autonomy_ from UI to DB
	- Always problems with database migrations, or UI, so better to have the power
	to work on that yourself
- _Reduced_ runtime _dependencies_ to a minimum
	- Hotfix, wait until the evening
		- With self-contained systems, a system has services that have the availability
		that is required for a good experience (some systems are not used that much,
		the customer doesn't lose money if some specific system is not operational for
		a bit)
- _Simpler_ Deployment and Operations
- Supports _continuous modernization_
	- In a few years, SCS by SCS can be modernized, which can then be way more
	sustainable

== On Designing an Observability Platform

There are tons of observability ecosystems, but one we can focus on because of
its popularity and easiness to pick up is: **The Grafana ecosystem**

- Grafana
- Grafana Loki
- Grafana Tempo
- Grafana Mimir
- OpenTelemetry Collector

Often referred to as the LGTM stack.

An observability platform can have different workload patterns competing for
resources:

. Metrics (high cardinality, steady writes, bursty reads)
. Logs (very high ingest + storage heavy)
. Traces (high fan-out, memory sensitive)
. SQL Server querying / dashboards (interactive, latency-sensitive)

And if you are going at full mega ultra scale, yeah, complexity grows to accomodate
for the thousands of needs. However if you are small, you can still benefit, and
actually is better if you shift left. That way you can grow way more easily.

Say you have about 100 transactions a day. Scale is nowhere near a problem.

This could be categorized as:

- "Foundational observability maturity" stage

At this stage you can build an **observability platform** correctly _before_
scale forces compromises.

The right goal for you is not:

- Hyperscale architecture
- Distributed telemetry clusters
- Aggresive sharding
- Massive retention engineering

The right goal is:

- Operational clarity
- Low maintenance
- Excelleng debugging visibility
- Future scalability without re-platforming

That leads to a very different recommendation.

**At this scale:**

- Traces are tiny
- Metrics are tiny
- Logs are manageable
- Query load is almost nonexistent

So the biggest risks are NOT:

- CPU
- Ingest throughput
- Disk IO

The biggest risks are:

- Operational complexity
- Poor telemetry design
- Overengineering
- Lack of standarization
- Observability nobody actually uses

_And this is extremely common in small/medium enterprise telemetry rollouts_

**The most valuable asset right now is NOT hardware_

It is:

**Telemetry consistency**

Meaning:

- Standarized trace names
- Good span attributes
- Correlation IDs
- Structured logs
- Service naming conventions
- Environment tagging
- Transaction identifiers

This is what makes observability powerful

Not 64 CPUs.

[IMPORTANT]
====
Build a "Small but Correct" Architecture
====

[cols="1,1"]
|===
|Component | Recommendation

|Grafana
|Yes

|Loki
|Yes

|Tempo
|Yes

|Prometheus or Mimir
|Prometheus initially

|OpenTelemetry Collector
|Absolutely yes

|Podman
|Good choice

|RHEL
|Good choice

|Object storage
|Optional initially
|===

=== Mimir and Prometheus

Mimir => Is operationally heavier, designed for scale, more distributed in
philosophy.

**Plain Prometheus is simpler and better for small scale**

Prometheus is:

- Easier to reason about
- Easier to back up
- Easier to troubleshoot
- Extremely capable at your size

Migration can come later.

A recommended stack would be:

[cols="1,1"]
|===
|Service | Purpose

|Grafana
|Dashboards/UI

|Loki
|Logs

|Tempo
|Traces

|Tempo
|Traces

|Prometheus
|Metrics

|OTelCollector
|Telemetry gateway
|===

This architecture is **Strong**

Because it gives you:

1. Full Correlation

You can move:

- Metric -> trace
- Trace -> logs
- logs -> metrics

This is where observability becomes truly useful operationally

If you decide to make usage of OTeL, you are adhering to a standard, which:

- Future-proofs the platform
- Avoids vendor lock-in
- Simplifies onboarding new services later

The direction of all different platforms is heading towards that.

A shot in the dark:

**Very Reasonable Starting VM**

Single VM


[cols="1,1"]
|===
|Resource | Recommendation

|vCPU
|4-8

|RAM
|16-32 GB

|Storage
|250-500 GB SSD

|OS
|RHEL 9

|Containers
|Podman

|Filesystem
|XFS
|===

4 vCPU / 16 GB would probably work perfectly fine initially

Telemetry volume is microscopic.

Even if:

- Each transaction emits 100 spans
- 500 log lines
- Dozens of metrics

This would still be nowhere near stressing modern hardware

Resource Usage will ACTUALLY Come From

Mostly:

- Grafana itself
- Loki indexing
- Java/.NET runtime overhead
- containers
- retention storage

Not ingestion throughput.

**Bigger Design Concern**

_Retention and searchability_

> "Can I debug a production issue from 2 weeks ago in 5 minutes?"

This is where observability earns its cost.

And so, Retention Strategy:

[cols="1,1"]
|===
|Signal | Retention

|Metrics
|30-90 days

|Logs
|14-30 days

|Traces
|7-14 days
|===

=== Most Critical things to take into consideration

**Invest in Trace Quality Early**

That matters MUCH more than infrastructure.

A good trace should answer:

- What happened?
- Where did latency occur?
- What dependency failed?
- Which SQL query failed?
- Which customer transaction broke?

Without opening multiple systems.

**What Good OTel Instrumentation Looks Like**

Include attributes like:

- service.name
- service.version
- deployment.environment
- transaction.id
- customer.id (carefully)
- sql.server.name
- db.operation
- http.route
- exception.type

But avoid

- High cardinality junk
- Raw query text as labels
- Session IDs everywhere

> We're small so observability can be informal

No, the opposite is true.

Smaller environments benefit massively from:

- Rapid troubleshooting
- Low MTTR
- Operational confidence
- Faster onboarding
- Reduced dependency on tribal knowledge

High cardinality junk:: Number of unique label/tag combinations. You shouldn't
label user ids, customer ids, they can be inside something that indexes them and
it has low cardinality
Low MTTR:: Mean Time To Resolution (how long it takes to fix production problems)

And let's introduce an OTel Collector Centralized Initially, that way, even if
we have to scale eventually, it will have to be simply adding a new gateway:

````
Services
   ↓
OTel Collector
   ↓
Loki / Tempo / Prometheus
````

- Centralized processing
- Easy config changes
- Batching
- Retries
- Normalization

And later you can split into:

- Agent collectors
- Gateway collectors

Without redesigning instrumentation.

**Recommended “Day 1 Production Shape**

- VM
	- 8 vCPU
	- 16–32 GB RAM
	- 500 GB SSD
- Containers
	- grafana
	- loki
	- tempo
	- prometheus
	- otel-collector

- Networking

Internal-only initially.

Backups

Back up:

- Grafana dashboards
- provisioning configs
- Loki/Tempo storage

**Good starters for focus**

. Correlation IDs

This is huge.

Every request should have:

- trace ID
- transaction ID

flowing everywhere

. Structured Logging

JSON logs

Not free-text chaos

. Golden Signals

Monitor:

- Latency
- Errors
- Throughput
- Saturation

Dashboard Simplicity

Start with:

- Service health
- Failed transactions
- Latency percentiles
- SQL dependency health

**Old Style** was "monitor servers". **Modern Style** is "understand transactions"

That is why:

- Traces
- Exemplars
- Correlation
- OTel semantic conventions

Matter so much now.

Success should not be measured by telemetry volume, number of dashboards, infrastructure
complexity.

This should be what you think about all the time:

**"How fast can we explain a production issue?"

If observability lets you answer:

- What broke
- When
- Why
- Where
- Who was impacted

Within minutes, then the platform is succeeding.

=== Grafana Alloy

You don't have to build your own OpenTelemetry Collector, a "default answer" in
the Grafana ecosystem is: **Grafana Alloy**

It is a production-ready distribution of the OpenTelemetry Collector specifically
designed to integrate with hte LGTM stack.

- Runs beautifully in containers
- It is designed exactly for "small now, scale later"
- It avoids you having to hand-build a custom collector distribution

Be aware of 3 different meanings:

[cols="1,1"]
|===
|Meaning, Reality

|Build from source
|Not needed

|Create a custom distribution
|Not needed

|Write config pipelines
|Still needed minimally

|===

You WILL still configure:

- Receivers
- Processors
- Exporters

But you will NOT:

- Compile anything
- Maintain custom binaries
- Manage component compatibility

**The Industry Direction Right Now**

Historically:

- Grafana Agent
- OpenTelemetry Collector
- Prometheus agents

were fragmented

Grafana Labs clearly is consolidating around:

**Alloy as the unified telemetry pipeline**

Grafana introduced Alloy at GrafanaCON specifically as:

- "the OpenTelemetry Collector distribution with built-in Prometheus pipelines"

That matters because:

- Docs are improving rapidly
- Ecosystem support is growing
- Examples are plentiful
- Integrations are first-class

**One Alloy Container is enough for small**

````
Applications
   ↓ OTLP
Grafana Alloy
   ├── Loki
   ├── Tempo
   └── Prometheus
         ↓
      Grafana
````

_Note:_ Some advanced users say that Alloy might fall behind on some features
and whatever, but that's irrelevant for more use cases unless you need bleeding-edge
processors, hyperscale infra, uncommon exporters.

This would be a great start:

[cols="1,1"]
|===
|Container | Purpose

|alloy
|Collection pipeline

|grafana
|Dashboards

|loki
|Logs

|tempo
|Traces

|prometheus
|Metrics
|===

Alloy Features that matter operationally are:

1. Unified Telemetry = One collector, logs;metrics;traces. Instead of multiple agents
2. Built-in batching/retries = This matters during outages
3. Memory limiting = Critical protection against collector crashes
4. Native OTLP support = Applications can emit directly using OTLP/gRPC and OTLP/HTTP
5. Prometheus compatibility => Prometheus ecosystem remains massive, exporters are everywhere, infra metrics become easy later

**REALLY IMPORTANT RECOMMENDATION**

> Standardize Semantic Conventions Early

Use:

- service.name
- deployment.environment
- service.version
- db.system
- db.operation.name
- http.request.method

This makes dashboards reusable later

=== Why does observability exist?

It's about **REDUCING MTTR**

Not collecting pretty graphs.

Good observability helps teams answer quickly:

- What broke?
- Where?
- Why?
- Which dependency?
- Which deployment?
- Which SQL query?
- Which service?
- Which customers?

== Fundamentals of Backend Architecture - How to Design Scalable Software

Notes from: https://www.youtube.com/watch?v=Qa-7iWxDz1A&list=WL&index=8

When it comes to Software Design, Martin Fowler is a great initial starting point.

Objective: Create a Google Drive clone.

Rate Limiting, Caching, Horizontal Scaling.

There are multiple ways of designing software, the idea is to think in systems.

[IMPORTANT]
====
A good architecture is important, otherwise it becomes slower and more expensive
to add new capabilities in the future.
====

=== How to design software

Gradual evolution => Avoid over engineering and intentional progress

**This is a heavy topic**.

**First Step:** You have a server. You have users.

The users make requests to your server => So all Data inside the server.

- What happens if the server dies?
- What happens when you want to scale?

If the data is inside the server: If the server dies, the data is lost.

Second step? Add a database.

You have a server, and a database. When we make requests to the server, we get
the data from the database. (Data persistence)

What happens if the server dies? => We have a persistent layer that fights that.

When it comes to scaling, say you want to server more users, because you already
separated/decoupled a database, then you can clone many instances of the server,
and no issues will arise, because the server takes care of business logics solely.
All the **new instances** will be looking at _one database_, this is the separation
of concerns, the `database` doesn't actually know that there's a server, it just
cares about storing the data, then the server indeed knows about the database,
but because you have decoupled server from database, then if one component dies:

**The other component still lives on**

Concept: Data coupled to the machine = no scale, no resilience

=== Step 2

An application grows, more users start joining. Then complains start to rise up,
the response has become slow, so we now have to scale the server to more than
one instance.

**Load Balancer** => Piece in the middle will be inserted to redirect user requests
to healthy machines / round-robin algorithm. Another extra component that helps scaling,
**AND** still adheres to the principle of _separation of concerns_

Round/robin is good to start with, but if you start going deeper and try to fine-tune
things, you will come to this realization:

- An upload <> A simple GET query

There are operations, that different users might start, and that are way heavier
than others. So one server serving the really CPU intensive tasks (GETS) or
disk/database intensive tasks (FILE UPLOADS) will indeed have different "health
status", depending on what he is currently serving.

Through `healthchecks` you can enable your `load balancer` to look at the two
servers, and determine which one is the healthiest, so that he redirects the
traffic there, in hopes the user gets a better experience than if the request were
to be sent to the other less healthy server.

**There are different techniques, different load balancer solutions**

In the case of the heavy file upload, you could even decouple a "server" to
become a fully specialized master at uploads. And the load balancer can then
be configured to do `routing` based on special pathnames and rules so that if
he detects that a file upload is going to take place, he will **always redirect
the request to the specialized upload master**.

`POST /upload` => Always FILES server.

Here are other strategies:

- Weighted Round Robin
- Sticky Sessions (Session Affinity)
	- If a user comes on his first request to one specific server, you might want
	to keep redirecting him there in consequent requests
		- State that's being created by the user might be faster (due to locality),
		caching, memory, and so on.
- Least Connections (Alternative)
How can you "code" something like this?

- Put nginx as the load balancer
	- An `nginx.conf` is more than enough
- Create some servers in `go`

With a `docker-compose.yml` you can easily lab this idea, and how to configure, how
to program, and other playground purposes.

- Horizontal scaling = You add more machines
- Vertical scaling = One machine you scale it up (make it beefier)

Load balancers, can again help out, giving them the capability of **auto-scaling**.

`min=2`, `max=10`.

Auto-scaling can even do everything for you. You don't have to really do anything.

=== Microservices

If you _keep scaling_, _more users_, _more needs_. Things get again slower.

You might want to look at **microservices**

The idea is to use this architecture in **really big things**. That way one team
only focuses on a specific feature, another one on another, and so on. Highly
specialized teams, tech stacks and business knowledge.

Microservices could be divided like:

- NOTIFICATIONS
- FILES
- Auth
- Realtime

(These can be referred as different domains)

**Question:** Authentication, can it really just be a new service? Do we have
to go through the Load Balancer (unauthenticated)?

Not really, here comes the **Gateway**

- He can coordinate the distribution of requests and responses
	- He can even become an aggregator, say a user asks for one thing, but this
	actually has to go to many services or take different pieces of info, the
	Gateway can abstract away that complexity and take all responses and do one
	response to the user.

For security purposes also, the Gateway can be this **one and only point of contact**
for the outside world. All your infrastructure can then be put in a VPC (Virtual
Private C)

_However the Gateway can become a single point of failure_. Unless we scale it,
we can both scale the Gateway horizontally and vertically. (Which would move the
Load Balancer before the Gateway)

=== Object Storage

Once multiple services start communicating amongst themselves, then things get
even more complicated.

Databases => Are not meant to store files.

You should not directly upload to your server.

You can get `timeout`s, security might end up compromised.

So, separate the upload into smaller steps:

- Make an upload request
- Go to the Gateway
- Gateway calls a Files service
	- This creates metadata in database
	- And then we request from an Object Storage, a link. Upload link.
		- We return 200 with the metadata, and then we redirect the user to the
		upload link (the link can be Authenticated also)
		- And this Object Storage component takes away work from the "Files service",
		the architecture is untouched, a "backdoor" is established amongst the
		client and the Object Storage

Now, the upload to an Object Storage can take time, and after it's done we indeed
might want to react, **this shouldn't be through a synchronous call**. It can
easily turn into a mess:

- What if the service is not available?
	- What if the service is down?
- What if we want to fan-out to other services as well?

That's when async (pub/sub) communication can come in, and help out. Decouple the
Object Storage from services, and just put a bus/queue in the middle. So that
services listen, and then react on their own. (You introduce a Broker). The
Object Storage can then publish to the broker.

- Broker has to be highly available
- Durable messages
- Redelivery functionality
- Separation of concerns

If a message is not consumed, then a graveyard of messages can be created (DLQ)

=== Caching and CDN

(And rate-limiting)

What if the product still keeps scaling?

Optimizations need to come in place => You shouldn't try to do this at the beginning,
it's over-engineering. (Hence tutorials, youtube videos give a false preconception
to people of what they actually need, and what they should do)

There are multiple ways to optimize things.

- Caching => This can be a layer (Redis) between the database and a service
	- Cache lives in RAM, and to get info from RAM is way faster than disk
		- Nowadays RAM is super expensive, so BEAR that in mind
		- Also, an example use case is Netflix, that a new movie that is
		going to be popular comes out, they just put it straight into RAM

- One first layer of caching => File metadata
	- Because it's tiny, and rarely changes
- Caching file bytes in Redis is usually wrong = Expensive
	- But there's an alternative for it called **CDN**
		- This is a cache that can be used for **big things**

You can have a key for a file, that's still warm (in the cache). And then retrieve
it from there. And with the `cache-aside` pattern you can then put the database
retrieved in the cache for later usage.

**CDN**

It's a bit the same as the cache. Imagine that CDN has small points of presence
around the world. A CDN provider has more presence around the world. So, instead
of getting to your specific server or your location's storage, you can have
"copies" of your server around the world.

[IMPORTANT]
====
Video doesn't really explain what CDN's really cache. A CDN's purpose is to bring
your website's content closer to the user instead of making every request travel
back to your origin server.
====

_So a CDN mainly will cache these things:_

- Images
- CSS
- JavaScript
- Fonts
- Videos
- PDFs
- And sometimes HTML pages/API responses (this is called edge computing, but it's
nieche)

The main mechanism a CDN works under is this:

- A request is made, the edge server (the CDN's closest server to the user) tries
to find some cached info based on the request. If he can't find it, he goes to
**your server** gets as much as he cans, and caches it for next time.

**Rate Limiting**

- Malicious users can exhaust the resources of your system
	- It can affect your business heavily
- Rate limiting can work with the **CACHE**
	- Before your **Gateway** you can put a component that acts like a bouncer
		- He can look at the user that's making the request, and he can then save
		a footprint on the IP/NAME of that user in the cache
			- The more requests that the same user makes, with the cache you start
			analyzing if you will allow him to go through or not, if the user has
			made too many requests, then the _"bouncer"_ can just deny him from
			going to the server

== Someone was laid off by Attlasian

Notes taken from: https://www.youtube.com/watch?v=55pTFVoclvE&list=WL&index=8

Envoyproxy => Replace licensing cost proxies with this. (More modern nginx)

AWS CloudFormation Templates => IaC

Some statis: 2000 proxies - 13 regions

AMI => Standard image for the proxies. (Machine image)

Hashicorp Packer => SaltStack Configuration => AWS EC2 provisioner

SaltStack => Configuration Manager => I want to run packages, configurations,
stuff in a particular way, particular order. So declarative configuration for
servers. (Automated)

Install & Configure:

- Envoy
- Observability agent
- Security / Hardening
- Network tuning
- Containers

Esentially a cool idea for a provisioning system was:

- A provisioning API (FastAPI, Python) that talks to provisioning APIs and then
saves some data
- A second system that helps manage all this provisioning (connects to the database
that the provisioning API handles)
- And then this all interfaces with AWS products and things, following standard
patterns, standard language that is easily understandable and portable enough so
that everything "just werks", and of course tons of _industry standard_ features,
that cover a range of requirements are covered, and automated through the home-grown
systems. (This took 2 years)

- This system was the building blocks of a team.
	- It was centralized load balancing management
	- All of the features that were provided to the customers were defined in
	the templates
	- So basically, proxies that reacted dynamically to services that were being
	deployed over time

=== Larger Products should use the platform components

The idea was to migrate all microservices at Attlasian to this platform. Even the
larger products and systems.

This was easier, because the platform was built in such a way that standarization
was enforced by the platform usage itself.

The idea was that every basic service was getting its load balancing wiring automatically,
but it also made it so that the service is completely shut off from the basic
load balancing that was in place. (It had to go through the centralized load balancing
system).

It was a **hard rule**, if you did all of this for your service, that meant that
**you wanted your service to be publicly available**. Previously, it could've been
accidental that it was public, with the platform, **that was not a thing anymore**.

[IMPORTANT]
====
The biggest value added by the centralized proxy management system. Was to take
away, abstract things from developers, so that they have more time to focus on features,
and things that add value. The system is all about **dynamic configuration** because
developers only know **Templates**, and those abstract all the configuration that
has to be done on the Cloud Providers and Provisioning tools as well.
====

There are cross-cutting concerns, that for developers, adds more time, and features
get delayed, people are unhappy, clients even may have issues:

- DDOS Protection
- Access logs
- Authentication
- Authorization
- Rate Limiting

`Envoy` proxy has SO much configuration and features, that it allowed to take
on some of these concerns **away from the developers**.

DDOS Protection => CloudFront

[NOTE]
====
Validation was like **mega-hardened** on the provisioning API so that no wrong
provision or infrastructure was spun up. Based on the developer `parameters` then
**more templates were created dynamically**
====

- Devs => Set a bit of json
	- The system generates all the Envoy proxy configuration

For

- Authentication
- Authorization
- Rate Limiting

All of these had to run as side cars. They were their own services running locally
on the proxy server. (Containers essentially). Some of the side cars were made
by other teams, others were created by the Centralized Proxy Management System team.

- Authentication => Written in Rust

All of these services that lived in the proxy locally, were also configured and
provisioned. By the dynamic provisioning configuration that then fed onto the
AMI => Machine Image.

Cool result:

**Programmable proxy with sidecars that have their own separate logic from the
proxy and they can _too receive configuration over the wire_ - locally**

All the concerns were solved before backends being hit. The solution also was
done super quick, so that was even better.

**Non-technical requirements**

- Compliance

_Boring check-listing work, no new build_

- Diplomacy, conflict resolution, persuade, teach, educate, mentor => Skills
- Ability to maintain software and systems
	- See where the cracks show up and build things that don't show those cracks,
	or that show up way later

_At the very start, you have the requirement, onboard, document_. Where do things
break? Where things can automate?

Example:

- What if SQS stops working?
- What if Amazon goes down and we don't have db?
- What if wrong configuration is generated?

At the start there's a lot of these questions.

- People come and go
	- You have to do onboarding again
		- You should have more people
		- More opinions come in, things changing, things "become better"

**Churn:** It becomes predictable. Once you notice it. It's a smell, it's an indication
that that part of the service/project is going to keep increasing. And you have
to **take a decision**.

Maintenance burden => Doesn't appear at the beginning.

**Building something is easy, changing it, and make sure it's easy to change it
over time is hard**. Things end up coupled, and you have to assume those risks.

[NOTE]
====
Can AIs help untangle balls of mud?
====

Everyone has different styles and personalities (ways of working). Even though
conflicts arise, you still respect someone.

Sometimes your personality doesn't mix with their personality. Have self-awareness
and understanding of the other person (psychology and professionalism). You should
handle conflicts, anticipate conflict, make something to make the relationship
work.

_At times these conflicts affect performance_

Next time those situations come around, you should be able to handle conflicts better.

**Mentoring**

Easy is to help, point out areas to deliver understanding. Help with mental models.

- _Mentoring is a bit different_
	- Personally difficult is striking the balance between how much time do you
	give to the mentee
		- Don't give answers to problems
		- But also don't give them entire blockers all to the mentees

Training => That's different and probably easier.

== Microservices, Where Did It All Go Wrong - Ian Cooper - NDC London 2025

Notes taken from https://www.youtube.com/watch?v=d8NDgwOllaI

There's a paper, and it had impact on the industry. For a while it was really
popular, but later a backlash came in.

_Microservices:_ Love letter to microservices, and how people should go about it.
And how to fix your journey towards using them.

Fundamental theory of software engineering:

_How do we get to do faster software development?_

[IMPORTANT]
====
Due to complexity: It is always easier (and cheaper) to create two small pieces
rather than one big piece if the two small pieces do the same job as the single
piece. **The cost of a computer system will be minimized when the parts of the
problem are:**

- Solvable separately
- Manageably small

_Of course, everyone has a different definition of "manageably small"
====

**Module:** A script, bounded by boundary elements, with a name. We can break the
software into pieces, give it a name, and contain some amount of code in that space.


_Software components are things that are independently replaceable and upgradeable_

- Components come in two forms: Libraries and services
- Components relate to modules, but are replaceable
	- If you have a module with a nice interface, you can swap out things

**Service:** A component that exists in its own process, clients talk to it
over some inter-process communication mechanism.

The upgrade/replacement of one service should not require coordination with another

**Microservices:** A set of practices for successfully building a _software component_
architectural style (at scale):

- You are going to use components (independent components)
	- Independent replaceability
	- Independent deployability

Messaging => Makes things easier because of that.

- Organized around Business Capabilities
- Smart endpoints and dumb pipes
- Design for failure
- Infrastucture Automation
- Decentralized Data Management
- Decentralized Governance
- Products not Projects
	- A product manager cares about a service, and moving that forward, nothing
	less

**Isolation to the process model**


_Benefits_

- Strong Module Boundaries
- Independent Deployment
- Technology Diversity
- Fault Tolerance
- Cognitive Load (Code that fits in my head)
	- The micro refers to

_Costs_

- Distribution
- Verification
- Operational Complexity
	- Cost of operating the services goes way up
- Skill Issues
	- Need to do upskilling
- Data: BI & ML

=== Microservice Premium

Don't even consider microservices unless you have a system that's too complex
to manage as a monolith

- Code doesn't fit in your head
- A Five-Pizza Team
- Sub-domains have different forces nor change
	- Everyone is working at the same point, and they are getting in the way
- A wide range of interactions

[IMPORTANT]
====
You are solving **large** problems
====

=== The Problem

Relates to the name. Microservices => Badly chosen term.

_Micro:_ 100 lines of code services, if it's more for one programmer to maintain
it's not a microservice.

_Nanoservices_ Death-star diagrams => All the connections between your services.
The more things you have, the more interactions, the more integration points,
you massively increase the cognitive overload. **Latency increases**

- High Operational Overhead
	- Deploy scripts, telemetry
		- Every piece of metadata needs to increase by the number of items you add
- Synchronous Orientation
	- Things become heavily coupled
- Low Cohesion, High Coupling
- Long Release Cycles
- Obstacle to Reasoning

_What things do I need to deploy to make this work?_

The whole thing becomes a house of cards

_Services:_ Process is **container**, FaaS

Prime Video: They had an architecture with Lambda Functions, and they decided
to re-write that as a single service that's in EKS.

_From microservices to monoliths_

However it was one service that changed its process module.

**Fracture Plane:** Team Availability

The idea is that people think that making new systems, and breaking things down
into multiple components, will just add tons of extra maintenance costs. Even
onboarding of new members might become more of a hassle, because there are so
many moving pieces.

_Cost of ownership:_ It's an order magnitude higher than cost of authorship.

_We don't need to get at authoring software_ => I need to reduce the cost of
owning my software.

The 4 + 1 View Model.

We have lots of mental models on how designing software. The problem is that we
have too many ideas sometimes tucked-together.

**Note:** Domain Storytelling => Modern Use Cases

Attribute Driven Design => Capture quality

**Logical Model:** What is the problem we are modeling? How does the business
look like?

_How do we find subdomains within a system?_

Find pieces, keep talking to the business as to how it looks like and abstract that
reality in the form of a system.

**Subdomains**

- Actor produces result on their own
- One-way information
- Different triggers
- Activities supporting something not in the picture
- Differences in language

[IMPORTANT]
====
Business models change. We add new ideas, new things develop. So changes to your
domain model will happen.

Model processes, do not model activities and tasks
====

We take the domains, the things we break down into. And we ask, "How are we going
to build that?"

A bounded context => The people, the process, the things you use to implement
a part of the business.

_Typically you try to align with domains, but because business changes, they
start to drift_

=== Tips

If you are uncertain about a domain or bounded context: **Use a monolith**

- A monolith might use modules in it (you will learn from it)
	- The system has a single CI boundary
	- No more than 35 devs/5 teams working on the monolith (10 pizza rule?)
	- Will need a monorepo

If you have clear domains, and complexity that can fit within your head

- A microservice for each of these domains
	- Change, replacement is cohesive
	- ACID requirements are no issues

More complex models:

- Is the code for the sub-domain larger than your head?
	- Can you find seam(s) within the sub-domain and split into multiple bounded
	contexts?
	- Beware of the complexity of intermodule dependencies
- If it's independently solvable each split within the sub-domain becomes a microservice

**Macroservice:** Modularity is used here to break the microservice into multiple
pieces.

- A service; may use components within
	- The system likely has more than one module (library or service)
	- Has no more than 20 devs/3 teams working on any microservice (5 pizza rule?)
	- May or may not have/need monorepo
	- Dependency management becomes a lot easier (though still not trivial) the
	fewer the services/repos - better observability, debugging

If there are multiple subdomains, and they are trivial (if you break them apart
you would run into _nano-services_). If you break them down then it's not worth
the costs.

- You can then combine into macroservices
	- Your teams, which actually are bounded contexts, will own it
	- Chatty components, and exchanging same data => Typically you can combine
	these components
		- Just because you chose microservices, it doesn't mean that you can't
		combine and make macroservices
		- **A smell is that you have to do SAGAs or something like that**, if you
		find that, you might want to combine things

When your components are services with remote communications, then refactoring
is much harder with in-process libraries.

12-Factor App

You look at scaling requirements for different tasks/processes. What are the
different process types and how can you scale? Based on that line of thinking
you will separate processes, it will give you a good guideline.

**Microservice is an application**

**Macroservice may consist of multiple apps**

**App or Macrokernel is a constitution of many processes**

[IMPORTANT]
====
When you code, you effectively don't want the story that the code tells to be
telling you about the infrastructure, that should be abstracted, it should tell
you the story of the business and its processes.
====

=== Physical

A big problem when thinking about microservices, is that we don't know how to
allocate and separate the concerns.

== Modular Monoliths and Other Facepalms - Kevlin Henney - NDC London 2026

Notes taken from: https://www.youtube.com/watch?v=4qfsmE11Ejo

**We're going all in on microservices?**

**Oh, really, why?**

**Monoliths are a mess?**

It's _modular_ => It's a **new** approach.

**DO WE UNDERSTAND MODULAR**?

Distribution of Software Developers:

People doing it for a short amount of time are **way more** than the people doing
it for a long amount of time.

There's a lot of experience out there on software. We are very poor at communicating
concepts sometimes, bad at history. And in software things can be overwhelming,
so much info, so much to learn.

[IMPORTANT]
====
I try not to make fun of people for admitting they don't know things.

Because for each thing "Everyone knows" by the time they're adults, every day there
are, on average 10,000 people in the US hearing about it for the first time.
====

If I make fun of people, I train them not to tell me when they have those moments.

- "Diet coke and mentos things"? What's that?
- Oh man! Come on, we're going to the grocery store
- Why?
- You're one of the today's lucky 10,000

_Some people only care about one architecture pattern or way of thinking, they
don't know anywhere else_

- And some people say "I only know C".

**There are fundamental concepts that overlap, things that can be re-used anywhere,
and be passed around from any realm of knowledge to another**.

Separate out the underlying principle that can be taken anywhere, and then there
are specifics that might not be used.

_That's when you realize that you know more than you do_

=== The myth of issues

How to deliver quality software in a timely manner? It has been figured out. Knowledge
**is** out there. But we are really bad at putting that knowledge out there for people.

- Monolith => Has been repurposing its meaning, with a negative connotation. People
were struggling with "large and indivisible". You'd want an elegant arrangement
in your architecture.
- Micro => Words drift, they move around, their journey is far and wide. However,
there's a strong suggestion when "micro" is brought onto the table.

**Moment of calibration:** Understand where people are "FIRST"

- Lines of code, **should not** be read as this productivity correlation. But it's
a good guide as to what kind of _developer experience_ you are going to be hit with.
Cognitive overload, dissonance, and so on.

- Microservices:
	- Independent in testing, development, deployability
	- We want stability, it should have an interface that should not reveal much,
	as little as possible. **Implementation should not leak**
	- Services should work together, yet we should not assume behavior on another
	side.

[IMPORTANT]
====
OOP to me means only messaging, local retention and protection and hiding of state-
process, and extreme late-bindings of all things.
====

_Small Talk_

This is the Unix philosophy => Write programs that do one thing well, and then
combine small programs.

- Wrap things in interfaces, that way things are independent
	- An interface is a contract to deliver a certain amount of service; clients
	of the interface depend on the contract

We had this idea of monolith, and using it as "negative". Then microservices were
this "unconditional good". Then _Modular Monolith_.

- Where you not already partitioning it?
	- You can choose your paradigm, MAKE YOUR CHOICE
	- 32-bit computing solved the issue of big source files

````
The design thinking required to create a good microservices architecture is the
same as the needed to create a well-structured monolith
````

**It does not give you modularity**. That is something that's been available for
decades. It gives another bunch of related stuff related to the runtime environment,
the deployment environment, it can reinforce aspects of your cultural structure,
team structure, how you choose to develop.

_It can_. It's a choice. It's not mandatory.

**If you aren't tuned to that, you will go past it**

There are monoliths way more maintainable, scalable, faster deployable than microservices,
it's not as "simple".

**If you can't build a well-structured monolith, what makes you think you'll make
a good microservices architecture?**

_MESSY MONOLITH_ => This is bad.
_Messy microservices_ => This is bad too.

_Modular microservices_, _Modular monoliths_ => That's what people wanted, but
didn't say it like that.

There are four general principles that we can work on:

- Clean separation of concerns
- Data hiding
- Modularity
- Well-defined interfaces
- Strong fault-protection mechanisms (*)
	- Hugely important for Mars Rover code, not other realms

Technology => Not all of it is fast changing. There are things that change **extremely
slowly**

There are principles and ideas, that are stable, and that we should focus on keeping
in balance.

=== Modules

What are the modules?

Develop => I see modularity through my code, the constructs of the programming language,
how the files are organized
Deployment => I can have units of modularity, that are the things that I put out
there.
Run => When I look at my memory, at the process tabe, do I see different units?

You can go through other stages, or well, domains of execution.

Modularity => It's not immediately apparent when someone is speaking about the same
thing.

_An architectural definition is something that answers three questions_

- What are the structural elements of the system?
	- There's a gap among the big things and small things in a system and its
	components.
- How are they related to each other?
	- We have an HTTP connection, I'm calling an object on a thread, I'm a class
	inheriting from another class
- What are the underlying principles and rationale that guide the answers to the
previous answered questions?

=== Tradeoffs

Let's talk about requirements.

- Functional - Non-Functional => Stop. This only says "not-x", not WHAT IT IS.

- Functional = Semantics. Stuff that normally is in User Stories. Something that
can be tested with UT and Integration Tests.
- Operational 
- Developmental = What's the developer experience? How is it to maintain the
system?

If you go hard on operational, then developmental might take some negative hits.
We should always trade. _More complexity, because of some operational things_

_BUT, sometimes you get good tradeoffs, in where you get a win-win_ And other times
(i.e., with microservices) things can become doubly-BAD.

[IMPORTANT]
====
Whatever architecture choice. Whatever paradigm we choose.

The modular monolith, of course, is using cutting-edge ideas from the early 1970s
around structured programming, which some of us are still getting to grips with.
====

We should care about the boundaries, boundaries exist at different levels. And
we should try to get them right. (How we consider our teams, how processes are
architected, how your code is laid out).

_The strength, and meaning of the boundaries is always present_

Back in 1972, Benefits expected of modular programming are:

1. managerial - Development time should be shortened because separate groups
would work on each module with little need for communication
2. product flexibility - it should be posible to make drastic changes to one
module without a need to change others
3. comprehensibility - It should be possible to study the system one module at
a time

- How much stuff do you have to deal with?
	- There's a pattern as to the bigger, the less tests, the more outages, the
	more you go home late

Coupling is a measure of the interdependence of modules. The higher the coupling,
the more likely one change in one module, will affect another module. We should
aim to minimize coupling.

Cohesion is a measure of the strength of association of the elements inside a
module. If you try to separate from a highly cohesive "whole" then you get sad
and depressed. LOOK FOR THAT before separating things.

_Everything has been said before; but since nobody listens, we must always
start again_ => Software is a _fashion industry_.

We shall care about "modules", and the relationships between these modules, how
be balance off against things like runtime needs, deployment needs, and all other
tradeoffs that you incur when separating/decoupling things too-much, or too-little.
